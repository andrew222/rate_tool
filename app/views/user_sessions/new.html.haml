.row
  .small-6.large-6.columns
    %h2.title= t("shared.login")
    .login_form
      = simple_form_for @user_session do |f|
        = f.input :email
        = f.input :password
        .password-forgotten-block
          = link_to t("shared.password_forgotten"), new_password_reset_path
        = f.submit t("shared.login"), class: "button small submit-btn"
